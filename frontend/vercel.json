{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "routes": [
    { "src": "^/api/(.*)", "dest": "api/index" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}